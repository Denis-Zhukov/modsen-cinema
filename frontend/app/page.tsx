import { NavItem } from 'features/NavItem';

const Home = () => <NavItem path="fck">Hello</NavItem>;
export default Home;

export const metadata = {
    title: 'Cinema | Home',
    description: '...',
};
